{
  "name": "chaincheck",
  "version": "1.0.0",
  "description": "Blockchain-based product authenticity verification system",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "validate-env": "node scripts/validate-env.js",
    "deploy:local": "npm run validate-env && hardhat run scripts/deploy.js --network localhost",
    "deploy:mumbai": "npm run validate-env && hardhat run scripts/deploy.js --network mumbai",
    "deploy:amoy": "npm run validate-env && hardhat run scripts/deploy.js --network amoy",
    "deploy:polygon": "npm run validate-env && hardhat run scripts/deploy-production.js --network polygon",
    "deploy:prod": "npm run validate-env && hardhat run scripts/deploy-production.js --network polygon",
    "dev": "concurrently \"npm run server\" \"npm run frontend\"",
    "frontend": "cd frontend && npm run dev",
    "server": "cd qr-generator && node server.js"
  },
  "keywords": [
    "blockchain",
    "web3",
    "product-verification",
    "anti-counterfeit",
    "polygon"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "concurrently": "^8.2.2",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@sentry/react": "^10.25.0",
    "dotenv": "^16.4.5"
  }
}
